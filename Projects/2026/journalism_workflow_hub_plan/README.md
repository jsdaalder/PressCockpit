---
type: project
project: journalism_workflow_hub_plan_repo_pointer
status: archived
updated: 2026-07-12
---

# Repo-Local Planning Pointer

This folder is not the canonical planning project for `journalism_workflow_hub`.

Use the workspace-level planning project instead:

- `Projects/2026/journalism_workflow_hub_plan/` at the newsroom workspace root, outside this app repo

Why this folder still exists:

- it preserves older in-repo references
- it makes the duplication explicit instead of silently leaving stale planning notes behind

Rules:

- do not use this repo-local copy as a live backlog
- do not update priorities here
- do not treat these files as the source of truth during implementation
- if this pointer ever drifts from reality, fix or remove it instead of reviving it as a second planning system

Canonical planning docs live in the workspace-level project:

- `docs/current_priorities.md`
- `docs/architecture.md`
- `docs/roadmap.md`
- `docs/backlog.md`
- `docs/verification.md`
